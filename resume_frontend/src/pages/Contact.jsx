function Contact() {
  return (
    <main className="min-h-[70vh] bg-base-100 px-6 py-16">
      <section className="mx-auto max-w-3xl space-y-6">
        <h1 className="text-4xl font-bold">Contact</h1>
        <p className="text-lg leading-8 text-base-content/80">
          This portfolio project can be extended with authentication, saved
          resumes, and email delivery. For now, run it locally and generate a
          resume directly from the app.
        </p>
      </section>
    </main>
  );
}

export default Contact;
